server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mentorflow
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
